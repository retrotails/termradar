add "save" option to save default configuration, will only be used when no commands are given

add precipitation colors

check age of images, suggest refreshing

add some sort of high-color mode

animate by remembering difference between frames
automatically save frames with timestamp to play in a row,
	with blended frames?

halfres does not have contiguous lines

use control characters to allocate the space first, prevent glitching when the terminal can't keep up

to save people from having to open the SVG and look at coordinates, add presets for each state
	maybe interactive clicking on full map?

add option to change outline and pin colors

add ability to cancel without CTRL+C/Z

cache compressed .tif archives, don't redownload